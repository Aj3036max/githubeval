pipeline {
    agent any

    stages {

        stage('Checkout SCM') {
            steps {
                build job: 'pipeline-jenkinsfile1-checkout-SCM'
            }
        }

        stage('Install Docker') {
            steps {
                build job: 'pipeline-jenkinsfile2-install-docker'
            }
        }

        stage('Build Image') {
            steps {
                build job: 'pipeline-jenkinsfile3-build-nginx'
            }
        }
    }
}
